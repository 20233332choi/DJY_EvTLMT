# PR 검증 기록 (2026-09-21)

기준은 원본 `20233332choi/DJY_EvTLMT`의 `f8074a7`입니다. ZIP 기준 `d6c24d2` 이후 추가된 지도·휴대폰 GPS·기록 분석·공유 BMS 화면과 통합 실행기를 유지하며 수정분을 병합했습니다.

## 자동 검사

동일한 Linux / Python 3.10 / GCC 환경에서 `python3 -m unittest discover -s tests -v`를 실행했습니다. pyserial과 호스트 C/C++ 컴파일러, 로컬 HTTP/UDP 소켓을 사용했습니다.

| 코드 | 전체 | 통과 | 실패·오류 |
|---|---:|---:|---:|
| 원본 f8074a7 | 92 | 82 | 10 |
| PR 적용본 | 101 | 92 | 9 |

새 실패는 없습니다. 기존 rear UART 검사에서 발생하던 misleading-indentation 오류 1개가 해결됐습니다. 전체 테스트가 통과한 상태는 아닙니다.

남은 기존 항목:

- `test_board_time_sync`: 외부 `Documents/TV` 프로젝트의 시각 동기화 파일 부재로 3개 실패·오류.
- `test_front_timing`, `test_vehicle_frame`: 같은 외부 프로젝트 소스 부재로 각각 1개 오류.
- `test_stm32_imu`, `test_stm32_rpm`: 별도 `DJY_TQV` 저장소 및 고정 커밋 부재로 3개 오류.
- `test_tv_stm_esp.test_actual_sender_to_esp_and_usb`: 과거 후방 송신기의 enum/정수 조건식에 대한 호스트 C++ `-Werror=extra` 오류 1개. 해당 과거 제어 프로젝트는 수정하지 않았습니다.

통과 범위에는 바이너리 송신→ESP 실제 파서, CRC 손상/누락/삽입 후 복구, UART busy 버퍼 보존, FIFO/ACK/재시도, columnar 전 필드 복원, 1,100개 표본 전체 기록·다운로드가 포함됩니다. DB 쓰기 오류 시 묶음 전체를 rollback하고 ACK 없이 재시도할 수 있는지, 전력 프레임이 오래되면 다른 BMS 프레임이 와도 전력값을 유효하게 만들지 않는지도 검사했습니다.

## 펌웨어

- ESP32-S3: 예제 접속 설정으로 PlatformIO 빌드 성공. RAM 161,060 / 327,680바이트(49.2%), Flash 910,625 / 3,342,336바이트(27.2%).
- 후방 `firmware/stm32_rear_binary`: ARM GCC 14.2.1로 47개 C/시작 오브젝트 컴파일·링크 성공. text 48,516 / data 132 / bss 11,112바이트. 제공본의 FatFs stub 관련 경고 등은 남아 있습니다.
- 원본 반입 후 변경한 후방 5개 파일은 앞서 검증한 실차 작업 폴더 수정본과 바이트 단위로 일치합니다. 기존 TV/ED·IMU·안전·DAC 알고리즘은 제공본 그대로입니다.
- PowerShell 빌드 스크립트에는 `-BinaryRear` 선택을 추가했습니다. Linux에서 같은 소스·옵션으로 ARM 컴파일·링크를 검증했으며 PowerShell 실행 자체는 확인하지 않았습니다.

## 브라우저·조회

임시 SQLite와 합성 표본만 사용했습니다. 피트 13개와 BMS 11개 패널, 클릭한 표시값 유지 중 새 데이터 수신과 시간축 이동, 화면 일시정지, 선택 해제, 모바일 가로 넘침 없음, 분석 화면의 200개 단위 페이지 이동을 확인했습니다. 피트·BMS CSV와 분석 CSV·원본 JSONL 모두 1,010개 전체 표본을 반환했습니다. 원본의 사용자 정의 필드도 JSONL에 보존됩니다. 소스 JavaScript·Python 문법과 기능 변경분의 `git diff --check`를 확인했습니다.

`check_high_rate_web.py`의 브라우저 검사 중 피트/BMS 동작은 통과했습니다. 최신 분석 화면은 공개 채널만 담은 JSON을 표시하므로 구버전 원본 JSON 표시를 기대하던 마지막 assertion을 맞췄고, 분석 화면 및 최종 다운로드는 별도 브라우저·HTTP 검사로 확인했습니다. 수정된 브라우저 스크립트 전체를 다시 실행한 결과로 표시하지는 않습니다.

보드 플래시·연결 시험·실차 주행·실제 이동통신 전달률·BMS 내부 ADC 주기는 검증하지 않았습니다. 10 ms는 CAN 요청 주기이며 10 kW 초과 차단 기능을 구현하거나 보장하지 않습니다. 더미 서버·DB·개인 접속 설정·실차 CSV·빌드 산출물은 PR에 포함하지 않았습니다.
